/**
 * Re-export shim — canonical source lives in `packages/shared/`.
 *
 * Previously a byte-identical copy of `packages/shared/utils/formatters.ts`.
 */
export * from '../../packages/shared/utils/formatters';
