/**
 * Re-export shim — canonical source lives in `packages/shared/`.
 *
 * Previously a byte-identical copy of `packages/shared/types/whatsapp.types.ts`.
 */
export * from '../../packages/shared/types/whatsapp.types';
