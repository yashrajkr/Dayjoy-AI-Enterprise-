/**
 * Re-export shim — canonical source lives in `packages/shared/`.
 *
 * Previously a byte-identical copy of `packages/shared/types/api.types.ts`.
 */
export * from '../../packages/shared/types/api.types';
