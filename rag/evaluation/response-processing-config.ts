/**
 * @deprecated MOVED to `rag/response-pipeline/response-processing-config.ts`.
 *
 * Response processing is part of the response pipeline, not the
 * evaluation framework. This file re-exports the moved config for
 * backward compatibility. Update imports to point at the new path when
 * convenient.
 */
export * from '../response-pipeline/response-processing-config';
